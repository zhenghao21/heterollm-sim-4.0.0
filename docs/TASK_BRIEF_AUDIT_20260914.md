# 仿真器机制建模、可信度与泛化优化任务书

> **文档状态（动态）**：继续自动优化循环；哈希校验失败按第8节仅完整复核一次，并按失败阶段决定恢复范围。R37预测已完整执行，原评分失败保留；用户要求的五模型完整复核已通过，恢复评分与配对报告已生成，但独立严格验收因GGUF元数据头不一致返回证据不足；恢复链不再重试。R39同次读取绑定修复已独立审查并新冻结，131格预测、评分和证据完整性已通过；严格准确性仅6/131格达标，有条件保留继续机制优化，暂不提交。尚未通过误差验收。R36/R38结构检查不代替精度验收。验收主口径为engine-first，client仅作二级诊断；稳定章节定义规则，动态章节原位替换。
>
> **适用项目**：`F:\\codex_project\\37_LLMsim\\heterollm-sim-4.0.0`
>
> **当前目标**：A门为固定131格每格三项Engine误差严格<10%的开发回归目标；B门为独立数据、计时证据和统计不确定性合格后的正式准确性验收，仍采用逐格三项<10%。A门通过不得称总体完成。阶段轮数仅为复盘点；当前A、B均未通过。

## 1. 总目标与优化优先级（稳定）

根据模型结构、权重格式、输入输出长度、硬件、运行时和调度配置，生成可解释的算子图与执行时间预测。仿真对象的一级范围是 engine 内部的 GPU、HBM/HBF 与其他内存访问、kernel、KV cache、CPU/GPU 协作、同步、采样和调度执行；这些部分共同决定研究对象的 engine wall。发生在 engine 计时边界之外的 HTTP、JSON、tokenizer、admission、队列、SSE、socket 及其线程调度属于 server/client 服务边界，作为分解后的二级诊断。边界内的 host 建图、CPU 采样、驱动提交、同步及其调度等待仍属于 engine wall；按源码计时点、实际资源和重叠关系确定归属，不能因其发生在 CPU/OS 就一概移出 engine，也不能把所有宿主等待加成 GPU kernel 常数。对同一请求，服务侧只可在边界证据证明不存在重叠时写成 `T_server = T_frontend + T_queue + T_engine + T_response`，再将请求/接收传输单独计入 `T_client`；存在并发或包含关系时必须保留时间线，不能把各段简单相加。HBF 参数扫描只能改变 engine 内受其影响的内存、KV、kernel 和调度成本，不能把不受 HBF 影响的 frontend/response 固定开销吸收到 engine 成本。预测不得读取或拟合待预测场景的原生端到端时延。

优化顺序固定为：

1. 先保证执行语义和测量口径正确；
2. 再改善未见场景的预测误差；
3. 最后降低校准成本和模型复杂度。

任何降低平均误差但引入数据泄漏、错误语义、证据缺失或覆盖率下降的方案都不能保留。

## 2. 迁移能力与允许信息（稳定）

| 迁移类型 | 允许使用的信息 | 明确禁止 |
|---|---|---|
| 同硬件与运行时，换新模型 | 新模型结构、权重格式、静态 shape、已冻结的通用算子性能模型 | 用新模型完整推理时延重新拟合；补测专属场景后宣称“零校准迁移” |
| 换硬件或软件运行时 | 设备规格、预先规定的通用算子/内存/传输/调度微基准 | 用目标 LLM 的 TTFT、TPOT、E2E 拟合硬件参数 |
| 全新硬件，只有规格 | 规格与已有机制模型 | 与经过硬件标定的预测混用同一可信度等级；必须单独标记“规格驱动、未验证” |

profile 的 **provenance**（采集时实际 binary、DLL、硬件、运行时和配置的来源真实性）与 **compatibility**（执行路径、构建配置和性能曲面能否迁移）必须分开判定。EXE/DLL SHA 和依赖集合是 provenance 门禁条件，不是性能等价证明；`profile_gate=blocked` 只禁止该 profile 进入正式预测，不改变原始 native actual 在其采集环境下的证据状态。跨 binary 只有在源码路径、构建参数和独立通用微基准证明等价，并写明 allowlist 适用范围后，才可按兼容性范围复用。

## 3. 三层建模约束（稳定）

### 3.1 执行语义层

确定实际执行的算子、输入输出 shape、dtype、layout、量化路径、输出行选择、KV cache 行为、设备放置、依赖顺序和重复计费。按锁定版本 llama.cpp/ggml 的真实实现选择模块；不得按模型名称加入时延补丁。`lm-head` 的行数由后端实际 logits 输出行决定，不能固定成 prompt 长度或一行。

### 3.2 算子成本层

成本写成

\[
T_{op}=f(op,shape,dtype,layout,kernel,hardware,runtime)
\]

允许使用独立微基准得到的有效吞吐、带宽、启动开销、缓存效应和局部性能曲面，也允许使用有物理解释且独立验证的残差模型。每个参数必须记录来源、单位、适用范围和失效条件。禁止以“模型 × prompt 长度 → 最终时延倍率”为主要机制；模型 SHA 和 prompt fingerprint 只用于溯源。

Roofline 只能作为基线和约束。GEMM 效率必须考虑矩阵尺寸、分块、并行利用率和 kernel 选择，不能只用峰值算力或峰值带宽。NVTX CPU range 或 API wall 不能直接等同于 GPU kernel wall；异步提交、多 stream 和同步造成的时间必须按关键路径分析，不能把 phase wall 减去 kernel wall 的差值机械地追加到算子成本。

### 3.3 系统调度层

根据依赖图、资源竞争、请求到达、batch 策略、CPU/GPU 协作和传输重叠生成时间线。可重叠的区间不能直接相加；stage wall、API wall、kernel wall 的包含关系不能重复计入。连续 batching、并发 prefill、decode 顺序和共享启动边界必须有 trace 或明确推导支持。

## 4. 统一计时契约（稳定）

一级验收采用 llama.cpp **engine 口径**；二级诊断为 server 边界分解，三级诊断为 client 完整链路。三套口径必须同时保存，不能互相改名、混用或用二级结果抵销一级失败。

engine 一级指标固定为：

\[
TTFT_{engine}=t_{first\ engine\ token}-t_{engine\ request\ begin}
\]
\[
TPOT_{engine}=\frac{t_{last\ engine\ token}-t_{first\ engine\ token}}{N_{out}-1},\quad N_{out}>1
\]
\[
E2E_{engine}=t_{last\ engine\ token}-t_{engine\ request\ begin}
\]

`engine request begin` 必须是请求已进入 llama.cpp engine、完成 server admission 的边界；`first engine token` 必须是 engine 完成首个真实 token 的采样/生成边界；`last engine token` 必须是 engine 完成最后一个真实输出 token 的边界。`engine request end` 若被保存，必须等于该最后 token 边界，不能包含 response queue 或 socket 写入。`prefill_begin`/`prefill_end` 只表示阶段范围，不能单独替代 `TTFT_engine`。原生 `prompt_eval_ms`、`eval_ms`、`total_ms` 只有在 extractor 明确证明与上述 marker/counter 同边界时才可作为一级字段，否则只能作为阶段诊断。simulator 必须输出相同边界的 prefill、decode/TPOT 和 engine E2E；一级误差只比较同边界的 engine 字段。若原生缺少 engine marker、源码证明的 counter 或逐 token 时间戳，一级该指标记为证据不足，不得拿 client 值或 `prompt_eval_ms` 回填。

server 二级分解用于解释服务开销，不改变一级边界：

\[
T_{server}=T_{frontend}+T_{queue}+T_{engine}+T_{response}
\]
\[
T_{client}=T_{request\ transport}+T_{server}+T_{receive\ transport}
\]
\[
TTFT_{client}=T_{frontend}+T_{queue}+TTFT_{engine}+T_{response,first}+T_{transport}
\]

其中 frontend 包括 HTTP/JSON/tokenizer，queue 包括 admission/排队，response 包括采样后编码、SSE/JSON 和 socket 写入；只有 marker 或独立微基准证明边界与重叠关系时才可估计各项。并发或传输重叠时必须按时间线计算，不能把包含的 stage/API/kernel wall 重复相加。HBF 扫描只作用于 engine 内 memory/HBF、KV、kernel 和调度项，server/client 固定开销保持独立。

客户端三级指标固定为（用于完整在线服务诊断，不作为 HBF/GPU 机制准确性的替代标准）：

\[
TTFT_{client}=t_{first\ real\ token}-t_{request\ POST}
\]
\[
E2E_{client}=t_{last\ real\ token}-t_{request\ POST}
\]
\[
TPOT_{client}=\frac{t_{last\ real\ token}-t_{first\ real\ token}}{N_{out}-1},\quad N_{out}>1
\]

首 token 必须是真实非空响应；`[DONE]` 等控制消息不计入 headline E2E，尾部单独记录。客户端排队、HTTP、序列化和流式发送开销不得吸收到 engine 算子成本；只有具备独立 server boundary evidence 时，才进入二级 client 解释。engine 边界内实际发生的采样、CPU/GPU 同步与调度执行属于一级范围，但必须由对应模块解释，不得混入 GEMM 或带宽倍率。

输入 token 数必须由锁定 tokenizer 确认。输出长度、EOS 策略、warmup、缓存状态、并发、batch/ubatch 和线程数写入证据。固定输出（`ignore_eos=true`）与自然 EOS 分开统计；自然 EOS 提前结束不能静默回填 requested length。

并发场景保存每个请求的 request/first-token/last-token boundary、token 时间戳、请求延迟和整个 batch 的起止及 makespan。增加 NVTX/CUPTI/NSYS marker 后须检查观测开销，profiling 运行不能直接冒充无扰动真值。

## 5. 数据集隔离（稳定）

| 数据集 | 可执行操作 | 可证明内容 |
|---|---|---|
| 开发集 | 查看 trace、分析误差、修改机制、拟合允许的底层参数 | 问题定位和机制开发 |
| 验证集 | 比较候选方案、选择版本、决定保留或回滚 | 开发过程中的泛化表现 |
| 最终盲测集 | 版本冻结后只读评分 | 冻结版本的独立验收结果 |

版本选择后，揭盲数据转为开发/回归数据，不能继续作为最终盲测。盲测预测必须先落盘并带时间戳、身份和 SHA，再揭示 native actual。冻结期间不得修改当前运行实际引用的代码、profile、微基准库、运行配置、测试清单、指标提取器或聚合脚本；若必须修改这些冻结输入，先封存原批次、停止派发新任务并等待已启动子进程自然退出，再建立新版本。允许在与运行快照隔离的工作区立即诊断、修复失败并准备下一候选；不能将修复结果写回旧冻结或混用不同源码的预测。历史 native actual 是其原始采集环境的独立测量记录；当前工作区变化或 profile 被阻断，不得覆盖、改写或删除该记录，只能生成带新 run_id 和来源关系的派生解析/回放结果。

## 6. 泛化分组（稳定）

正式报告至少按模型、硬件和迁移类型分组，并分别覆盖：

- **Shape 泛化**：未参与开发的 prompt/output/并发/batch 组合，含边界附近和区间外样本；
- **跨模型泛化**：完整留出模型，规模变化与架构变化分开；
- **跨硬件泛化**：先完成规定的通用微基准并冻结硬件模型，再测目标 LLM；
- **组合泛化**：新模型与新硬件或未见 shape 的组合。

相关维度必须联合覆盖，且 kernel 路径一致才可称为插值；域外样本显式标记回退、外推或不支持。随机拆分相关请求不能替代按完整模型、设备或实验组留出。

## 7. 验收阈值与统计（稳定）

对每个场景先计算 native 与 simulator 重复运行的中位数，再计算。一级先分别验收 `TTFT_engine`、`TPOT_engine`、`E2E_engine`；只有一级三项都达到目标后，才进入 server/client 二级、三级结果的机制解释与优化：

\[
e_i=\frac{|median(T_{sim,i})-median(T_{native,i})|}{median(T_{native,i})}\times100\%
\]

一级验收只使用同一 engine boundary 的 `TTFT_engine`、`TPOT_engine`、`E2E_engine`；二级按 server 分解证据报告 frontend/queue/response，三级再报告 `TTFT_client`、`TPOT_client`、`E2E_client`。缺少对应边界证据时只能标记为诊断，不能回填或折算。各层均报告有符号误差、绝对毫秒误差、每次运行误差和测量波动。场景误差 P90 与请求延迟 P90 必须分别命名。

`E2E_engine = TTFT_engine + (N_out-1) * TPOT_engine` 在固定输出策略下具有确定的代数关系，因此同一短输出请求的三项同时达标不能视为三个独立机制已经验证。任何一次性启动或边界残差都必须在未见输出长度上检查其对首 token、首个间隔、后续逐 token 间隔和 E2E 的影响；恒等式在每个请求上核验，三项跨重复中位数一般不满足同一代数等式，必须分别汇总实测与仿真，不能由两个中位数推导第三项。正式验收至少保留一个不同输出长度的留出场景，并报告每个 token 间隔的误差分布。一次性残差的来源只能写成待验证假设，除非源码和独立 trace 证明其触发条件、生命周期和重复次数。

| 验收项目 | 第一级：engine 目标 | 第二级：server 诊断 | 第三级：client 诊断 |
|---|---:|---:|---:|
| 每格三项绝对相对误差（A开发门与B正式门分别判断） | 每格TTFT_engine、TPOT_engine、E2E_engine均严格<10%，等于10%不通过 | 单独报告各边界，不设抵销一级的阈值 | 单独报告，不设抵销一级的阈值 |
| 每个主要分组绝对相对误差中位数/P90 | 保留诊断统计，不替代逐格验收 | 诊断，单独报告 | 诊断，单独报告 |
| 每个主要分组最大场景误差 | 三项均严格<10% | 诊断，单独报告 | 诊断，单独报告 |
| 固定验收范围有效预测覆盖率 | 100%；缺失或拒收格不得排除后宣称达标 | 不降低一级覆盖率 | 不降低一级覆盖率 |
| 纳入正式结论的证据完整率 | 100% | 证据存在时报告 | 证据存在时报告 |
| 跨模型目标模型专属时延拟合次数 | 0 | 0 | 0 |
| 跨硬件目标 LLM 端到端时延拟合次数 | 0 | 0 | 0 |

当前固定范围为stable_native_dataset.json锁定的131格，不能因预测困难再缩小；原始162格和31格稳定性排除仍单独完整报告。逐格误差按各场景重复运行中位数计算，使用未舍入数值判定；不要求每次重复的瞬时时延误差都小于10%。A开发门必须在同一冻结版本获得131/131格、393/393项有效且严格<10%的结果，失败、超时、身份异常、非有限值和证据不足均不能通过。固定集通过只代表开发回归目标达到，不代表独立盲测或跨硬件通过。

**A/B双门与可证明范围。** 不降低逐格10%要求，但将“开发目标”和“正式结论”分开：
- A门：固定131格点估计全部<10%，用于机制优化与回归，不能反复针对这些答案优化后称独立泛化。三次重复时的低波动只描述已观测样本；当前131格均只有3批次、formal_repeatability_accepted=false，不能据此证明总体测量误差<5%。
- B门：在A门之后冻结机制和所有输入，在预先登记的独立场景/完整留出模型上按相同逐格三项<10%判定；模型、硬件及支持域在揭盲前确定。根据实测独立实验批次建立并报告统计不确定性。若置信范围跨越10%边界，结论为“证据不足”，不能硬判通过；不将噪声从误差中减去，也不设置事后容差。
- 正式采样数量、独立实验单位、最多加样次数、联合置信水平（默认95%）及多重比较控制方法必须在新native采集前冻结。并发请求与token不能冒充独立重复；普通单项95%区间不能宣称393项同时95%可信。不能靠反复查看结果直到区间过线停止采样。样本不足时保留证据不足，不用3批次bootstrap制造虚假精度。
- A门通过后停止针对固定答案调参并冻结候选，转入B门；若独立证据缺失，标记“开发回归达标、独立验收未完成”，不能反复拟合A门替代B门。当前用户锁定native继续复用，本次审计不授权或启动重测；若B门所需新证据缺失，继续能独立完成的机制工作，并明确剩余采集需求，不重新拟合目标LLM。
- 固定131格的100%预测覆盖与原始162格的native可评价覆盖分别报告：后者目前131/162=80.86%。31格不稳定样本不进入现有误差优化，但仍属于未验证范围；不得写成整个162格或所有新输入已支持。
- B门通过仅证明预先声明且实际验证的范围；缺少第二硬件时跨硬件保持未验证，不作为当前单机目标永远无法完成的隐性条件。HBF参数扫描另外报告敏感性与未验证范围，不能从本机拟合精度直接推出新硬件准确性。


TTFT、TPOT、E2E 在一级 engine 口径下必须分别达标，不能相互抵消。失败、超时、身份不一致、低可信度和证据不足样本必须进入覆盖率与失败统计，不能静默删除或通过更换测试集达标。样本不足或波动过大时结论为“证据不足”。

## 8. 自动优化循环（稳定）

每轮采用自顶向下的诊断优先级，并保留三种对照：纯分析基线、现有校准模型、新机制模型。先区分“实际过程没有表达或关系建错”的结构问题，与“过程已经正确表达但局部服务成本不准”的成本问题；最终误差同号或偏低本身不能区分两者。engine-first规定主计时边界，本节规定该边界内的诊断顺序，两者不可替代。

系统级缺口排查围绕以下五项展开。沿用身份、配置和适用域仍匹配的已有审计证据，仅补查本轮受影响部分；不是每轮从头审遍整个系统，也不要求上层先取得某个精度分数才准许下钻。

| 诊断问题 | 核对内容 | 能区分结构与成本的证据 |
|---|---|---|
| 实际执行哪些阶段 | engine内主机准备、提交、设备执行、传输、同步、采样；设备与数据放置；冷启动与稳态 | 锁定源码调用链、实际阶段及数据搬运次数，不能仅检查模块名称存在 |
| 阶段如何互相制约 | 控制与执行依赖、异步生产与消费、流水与重叠、下一token触发条件 | 阶段顺序、CPU/GPU活跃区间、同步位置和batch形成过程 |
| 谁共享资源 | CPU控制线程、算力、显存带宽、链路、拷贝引擎和队列是否有共同owner | 资源映射与占用/释放时间；不能给同一物理资源重复赋予独立容量 |
| 什么状态保留或改变 | 权重/KV驻留、图与缓冲区复用，以及按进程/请求/batch/token发生的事件 | 状态转换条件及次数；源码声明不等于当前预测路径已使用 |
| 总时延如何形成 | 关键路径、依赖等待、资源等待、背压及包含关系 | 逐请求完成链与资源时间线，检查遗漏、错误重叠和重复计费 |

粗粒度计算图、设备放置和数据从哪里来、经过哪些链路、搬运几次属于系统级抽象；融合、kernel分派、tile与cache细化可在问题明确后继续下钻。多资源任务共同启动并不自动构成错误：必须结合上游任务划分审查是否保留必要先后关系、释放时机与重叠。CPU API调用、设备队列等待与kernel执行属于不同对象；先表达CPU生产并提交、GPU消费和同步阻塞的关系，再确定成本归属及服务时间，不能把同一API wall同时作为CPU提交与GPU等待重复计费。对已有证据支持的上层缺口，优先修正事件、状态、依赖和资源归属；上层未发现相关问题或证据指向成本时，再对关键路径上的局部服务定价。下层证据揭示新系统机制时返回修订上层。此顺序不增加验收门槛，不允许用局部成本调整补偿尚未解释的上层缺口。

1. 从开发集按模型/部署、误差符号、P/O/C切片、逐请求闭合关系及重复波动分层，首先按上述五项检查系统级机制遗漏或关系错误，记录“已表达、未表达、证据不足”及当前预测调用链。趋势比较固定其余因素，只比较完整匹配组，缺格和失败另计；系统性误差不是结构缺口的充分证据；
2. 先列出能解释误差的竞争性结构/成本假设，选择能够区分它们的最小证据，再为本轮选定单一、可证伪的机制假设。评估前由源码、事件状态与静态shape规定预计受影响集合、变化方向及应不变集合，不按看过的误差选择容易格；
3. 收集锁定源码、trace、独立微基准或推导证据，先核对现有成本所有权，避免重复收取nonflash、KV写入、logits传输、采样、launch和sync；
4. 进行最小修改并跑结构回归；实际microbatch的M、量化分派与物理KV视图均由真实语义决定，不能以并发数、模型名称或prompt档位替代；
5. 做同输入开/关成对消融，报告signed error、APE、绝对毫秒、共同有效格变化、覆盖率与失败。**任一基线/候选格执行失败，立即查因并定向补测；哈希校验失败时，对失败对象重新完整校验一次。** 保存首次及复核的命令、退出状态、摘要、长度、来源/运行库身份、原始异常和attempt；预期SHA不得改成新读到的值，局部块检查不能替代全文校验。一次复核预算按“冻结身份＋对象路径＋预期SHA＋首次失败事件”绑定；复核失败不产生可再次重试的新事件，不能换目录/进程重置预算或循环到成功。已由用户要求完成且对象、全文范围、冻结源码/运行库与预期身份匹配的复核可直接关联，不重复消耗资源。复核通过仅证明本次身份一致，不删除原失败、宣称根因已解决或证明历史执行正确；复核仍失败、长度/身份持续变化或已知数据控制异常时，停止受影响批次新派发并定位环境/读取器原因，其他独立工作继续。
   恢复范围由失败阶段决定：入口校验失败且尚未执行的任务，可在一次复核通过后另建恢复attempt启动；worker读取或预测结束校验失败时，不追认受影响预测，需在身份恢复后另立冻结/attempt重算受影响范围。**只有评分阶段的当前身份检查失败、且原预测入口/结束校验、worker终态和不可变预测文件引用均有效时，复核通过后允许复用原预测，以新的恢复评分attempt继续评分。** 恢复协议绑定原失败、唯一复核凭据、原预测/源码/配置/native选择和评分逻辑；不得修改旧冻结或补造旧评分。恢复评分仍执行原有全部门禁；同一故障在恢复中再现则停止，不再给该链条追加复核额度。评分器自身进行的身份检查属于评分执行而非额外失败重试，全部记录纳入证据审计，不能假定每轮固定只有两次身份检查。
   暂停受影响组新派发时，已启动进程自然退出；观察工具timeout不是实验失败，不因此终止或重启。确定性代码缺陷在隔离工作区修复后新冻结，未变格复用须证明等价，不能混拼版本。两组共有且非消融因素的缺陷应两侧同样处理；原失败进入历史/覆盖率报告。运行有效但误差超标的accuracy_failed保留评分并进入下一机制候选，不按哈希复核规则重复运行来碰阈值。
6. **单轮退化准入采用可复核的综合判断，不设固定退化百分比或加权总分。** 最终第7节逐格三项Engine误差严格<10%的目标不变。先核对执行语义、数据来源、计时边界、证据完整性与覆盖率，再审查收益覆盖哪些模型/部署/指标、最差分组是否改善、未参与本轮定位的验证场景是否受益；逐项列出退化的方向、绝对毫秒、相对误差、集中性及原已达标能力的丢失。不得用总体平均下降抵消某一模型系统性恶化，不得用错误语义补偿另一个错误或剔除困难格。区分“正确机制暴露既有缺口”“候选引入新错误”“证据不足无法判断”；前者可以有条件保留继续优化，后两者须补证或修正/回滚。独立于本轮定位的验证样本不足时明确报告，不把反复查看的开发格称为独立验证。
   每轮作出明确结论：①接受——确定性机制修复有证据，配对收益有覆盖，退化已逐项解释且未造成不可接受的能力损失，可进入第8步提交；②有条件保留、暂不提交——语义正确但退化解释或证据尚未闭合，写明遗留问题及下一项验证；③回滚——引入错误、主要收益不足或损害超过可解释收益。说明支持与反对接受的证据、取舍理由及适用范围，不能只写“总体利大于弊”，不因未达到最终10%目标就否定所有中间进展。证据完整性、冻结身份、失败留存和固定覆盖率仍为必守条件，不参与利弊抵消。低波动三重复不证明噪声下限，native最大偏离、CV和预测APE分开报告，不用噪声抵扣误差。
7. 原位更新任务书的结构性结论、失败原因与补测、验证结果、退化与下一轮顺序；过程更新保存在工作区，随本轮验收完成后的提交一起交付；
8. **仅在确定性缺口已完成优化，且基线组与候选组的误差改善验收通过后，才集中建立一次本地Git提交并推送GitHub。** 先锁定相同native选择SHA、131格清单、逐格配置、硬件/运行时/profile、计时契约和评分逻辑身份；两组只允许预声明的候选机制不同。依第6步综合审查作出“接受”结论后，验收分别核对共同有效格的三项误差、失败查因与补测、固定范围覆盖率及其他场景退化，不能靠三项平均抵消、改变分母或仅恢复输出宣称改善；只有两侧同格同指标均有效才计入配对改善。任务书更新、状态观察、临时诊断、准备工作或未通过改善验收的候选不单独提交。正确性修复若只恢复覆盖且未证明精度改善，保留在工作区并继续优化，不为改善误差回退到已知错误语义；其他未准入尝试保留证据并按结论保留诊断或回滚。单轮改善验收与第7节最终逐格<10%目标分开，最终目标未全部达到不妨碍已验收改善的单轮提交。提交时记录SHA与远端核对结果。
9. 开发对照先按内容冻结再运行，不要求为了生成快照先提交：记录基线提交、逐文件SHA/大小、候选补丁与不可变源码副本、配置及全部证据身份。未提交候选明确标注snapshot身份，禁止伪称来自已有Git提交；验收后提交时核对纳入的源码字节与受测快照一致，若内容变化须按影响范围重新验证。正式独立验收使用最终提交建立新冻结并在揭盲前锁定全部输入。
10. 每轮完成对照评估后复核第7节逐格三项目标，符合第8步提交条件的候选再提交推送；未达到且仍有可执行方向时自动进入下一轮，无需用户再次发送“继续”。阶段轮数和计划时间窗仅为复盘检查点，记录阶段效果和下一阶段预算后自动续轮，不作为停止理由。连续多轮仅完善工具而未改善预测时，必须复盘机制假设和证据缺口，避免无效重复；只有A、B两门均在声明范围通过、用户明确暂停或现有授权与资源下无法解决的外部阻塞才停止，仍能独立完成的工作继续。不得突破用户明确的费用、资源或时间硬上限。连续3轮若既未改善同一评估范围的最坏误差/达标格数，也未关闭预先声明的必要证据缺口，则自动进行路线复盘并切换有证据的假设；没有可执行假设时标记研究阻塞，不靠重复同一测量无限续轮。A门执行器tools/evaluate_strict_engine_goal.py已实现逐请求时间戳重算、全范围与冻结身份核验、严格阈值及缺失拒收；B门尚无独立验收实现，固定返回未验证。它输出下一步行动，不启动脱离当前任务的无限后台循环。

不得追加场景常数或全局倍率来掩盖系统性误差。

版本控制规则：按上述完成缺口优化与成对误差验收的时机提交，内容包含该轮代码、相关测试、任务书和必要的小型证据；在途子代理的未完成修改及其他独立工作保持隔离，不整体暂存工作树。GGUF、DLL/可执行文件、海量raw trace和native原始数据继续保留本机，不加入常规推送。失败或回滚也保留可追溯的结论；推送失败时保留本地提交和失败原因，恢复网络后重试，禁止强制推送或覆盖远端历史。

每轮还必须执行因果归因门禁：如果 profile gate、native 重采、extractor、simulator 或成本 profile 在同一轮同时变化，不能把误差变化归因于其中某一个开关。必须固定同一份 native payload 和同一组场景，分别运行纯分析基线、旧 profile（仅诊断）和身份/适用域合格的新 profile 三路 simulator-only 消融，并单独报告 native 分母是否变化。只有在 native identity、计时契约和 extractor 均固定时，才可把差异归因于 simulator 机制；旧 identity 的低误差与当前 identity 的失败只能作为关联事实，不能直接写成“门禁导致误差变大”或“旧 profile 掩盖了缺陷”。

## 9. 交付与停止条件（稳定）

交付必须包括：模型结构和假设、参数来源/单位/有效域/失效条件、冻结清单、数据集划分、原始测量与预测、分组误差、消融、失败案例、回归测试和一键复现入口。每条预测标记来源类型（机制分析、微基准插值、域外外推、条件回放）及是否在验证域内。

缺少第二种硬件或独立验收数据时，交付可执行协议并明确“未验证”。阶段轮数或计划时间窗到达时保存检查点并自动登记下一阶段，未达标继续。只有用户明确的硬预算/资源限制或不可解决的外部阻塞才暂停依赖工作并交付可恢复状态；不能放宽阈值、隐藏失败或把规格参数冒充实测验证。最终结论统一为“通过、精度失败、证据不足、尚未验证”四类；身份或重复证据失败不能冒充精度结论，但仍保留在分母中。

## 10. 当前版本与证据状态（动态，原位更新）

固定native为131格、894请求，选择文件SHA为cab8f3a4baa90f082f2fd83592065aabcb598e3d1b8b2732f21bc5f3e49df9c5。覆盖五个模型、六种部署：qwen25 17、smollm2 23、tinyllama 22、qwen35 22、qwen38 CPU 20、qwen38 GPU 27。原162格及31格既定稳定性排除保留，本阶段目标LLM重测0、专属时延拟合0。

最新完整候选为R34/on：131格全部预测、评分完成，0格执行失败，原20个失败格已在新冻结下定向补测成功，完整运行自然退出0。与R33/on原基线的111个共同有效格比较，333项Engine误差全部unchanged；另外20格只计覆盖恢复，原基线失败记录不改写。严格A门为5/131格、56/393项通过，126格精度失败、0格证据不足，有效预测覆盖率100%；A未通过，B与跨硬件未验证。尚无本轮配对精度收益，候选保留工作区，暂不提交推送。R27及更早结果保留为历史对照，不能用跨版本达标格数的差异替代同输入配对归因。

R27的两个off失败（qwen38_p512_o128_c2、qwen38_gpu_p1536_o128_c4）及R24历史SHA异常保持，不重试覆盖或追认；根因未定。独立同块OpenSSL/CNG诊断0003已执行串行一遍：14177个数据块与整文件SHA均一致，文件状态不变。随后因三次完整262证据核验超过辅助读取2GiB预算而拒收，四进程阶段未启动；旧attempt拒收保留。0004已按真实引用大小与检查次数预声明辅助读取上限5903482880B并通过54项主机回归；新的五遍模型读取上限仍74325580640B，计入旧一遍后累计89190696768B。新批次0002已自然结束：新的一遍串行和四遍并行均与预期SHA一致，每遍14177个chunk两实现一致，跨pass无差异；辅助实际读取5890978883B低于新上限。只能说本次未复现，旧SHA失败和预算拒收均保持，不能宣称间歇性问题已解决。

R25的on因配置证明错位在启动前拒收、0格预测；旧批次已封存。R27末层选行语义仍为锁定源码条件资格，不能升级为实际原生分派证明。R24与更早版本完整保留，均不是独立盲测通过。

## 11. 已完成修改与验证（动态，原位更新）

| 机制或基础工作 | 当前已完成内容 | 验证与明确边界 |
|---|---|---|
| 原生身份与冻结 | 全文SHA、长度、路径及freeze/resume一致性检查；迟到worker自然退出后一次性发布终态 | 身份一致不证明性能等价；历史SHA失败不被后续成功追认。180项生命周期相关回归及9项独立模拟通过 |
| 身份失败现场诊断 | 保留原单次4MiB全文读取与长度/dev/ino/size/mtime/SHA谓词；异常附同次实际摘要、长度、三处stat及四个分项结果，末尾stat异常也保留已得到的证据 | 9项新回归和6项原身份回归通过；预测器其他函数AST未变。未证明R32身份异常根因已解决，后续成功也不追认原失败 |
| 输出行语义 | qwen2/llama在最后FFN前选行；qwen35在全行final norm后选行；保留GET_ROWS与物理microbatch行数 | R27完整对照已评分，新增经验系数0；源条件资格不等于native实际分派证明 |
| CPU图生命周期结构 | 一个运行时context保留previous graph；区分known absent/unknown、reuse/miss、reserve与执行失败；在实际cohort成功后逐ubatch提交，模板cache不跳过转换 | 52项基础状态测试、13项新集成及43项相邻回归通过；独立复核无阻断。真实小场景开启/关闭诊断的请求、batch、事件及总时间相同；仍是partial/unpriced，精确输入roster和原生最后图身份未被证明，尚非有价格的DAG节点 |
| final norm设备与操作粒度 | 绑定自身norm权重；分别决定完整RMS与weight MUL的backend；处理CPU/GPU各自融合、分裂传输及逻辑读写。修复scalar block_size污染全局量化元数据，缺F32 activation证据时保留原分析回退 | 45项定向及此前185项相关回归通过；普通GGUF入口的分析回退保留，未新增经验系数。后续前端修复与本机制的组合另通过本轮148项联合回归，准确性尚待新冻结比较 |
| GPU消费者与前端依赖 | 非MTP在线target先保留CPU准备、展开body，再按实际GPU与唯一消费组生成传输/提交/CP；首次GPU输入传输等待对应ready，独立CPU前缀保留重叠；compact按真实等待边界细分 | 148项联合回归通过；M64首次Q/K/V各32768 bytes的传输等待正确，慢CP下详细DAG、compact与实际在线回放完成时间一致。通常只增1个CPU前缀stage；复杂初始交叠区域最多128个细分stage，超限显式失败并计入原验收分母。最终独立复核通过；新增阶段身份与保留阶段、MMU阶段分离，同一运行时连续两批回归通过。MTP与静态入口仍沿用旧路径 |
| 输出索引实际消费者绑定（R34候选） | host索引按物理invocation准备，实际GET_ROWS设备决定每目标唯一传输与依赖；保留CPU前缀、分支复用和模板重放 | 270项联合回归及两份独立审查完成；原失败代表格在未改冻结中取得同一故障的完整栈。原20格新快照补测全部成功，完整131格预测与配对评分已结束；共同333项误差unchanged，仅恢复覆盖，未通过误差改善验收，暂不提交；混合后端阶段回退仍明确保留 |
| MMVQ路径与数值 | 共享ABI纠正；连续布局wrapper与target动态路径和数值通过；Q4_K/M1/K2048/N2048独立target探针通过 | Q5工具shape在已有预测账本零命中；Q4账本覆盖45格但cache/layout未资格化，均未准入新性能系数 |
| 冻结成本对照与采集准备 | R30完成freeze/lock及4份131格预检；R31计时工具完成编译、69项主机回归和两项独立审查 | R30尚无完整预测；R31新wrapper实际路径与独立数值资格已通过，既定计时已自然封存：6有效、7拒收、87未执行，API关联缺失与warmup端点问题保留；服务成本仍未资格化，新增性能系数0 |

历史具体失败、回归计数、构建与逐字节归档见第15节索引及state.json，不把旧轮完成叙述反复追加到本节。

## 12. 最新误差及主要缺口（动态，原位更新）

当前证据完整的开发父版本为R39/on：131/131格完整，strict无证据不足，70/393指标通过，6/131格三项均严格<10%。其APE中位数为TTFT28.434%、TPOT25.950%、E2E24.468%，P90分别52.036%、47.001%、47.170%，最坏分别60.604%、56.471%、51.606%。与下表R34历史基线相比54格改善、69格不变、8格混合；13项退化集中在SmolLM2，1项E2E从9.366%升至10.970%。admission_review.0001.json已记录支持与反对证据，结论为有条件保留、暂不提交。R37恢复评分仍仅为历史诊断，不用于正式结论。R40因27B GPU P128/O32/C4 worker全文SHA不一致停止新派发，原进程已自然结束：120成功、1失败、10未执行，不能评分。identity_recheck.0001唯一完整复核已通过：14865116128 bytes三路摘要均为冻结157479，四项stat检查通过；复核额度耗尽，原失败保留，不能认定文件被替换或根因已解决。recovery.0001的11格定向补算已于2026-09-17 18:43:59 UTC自然退出0，11格均成功，恢复入口/结束五模型身份检查通过；原120成功格的worker退出、摘要与原入口/结束身份已核对，保留绝对引用不改写。恢复freeze为原freeze字节副本、source.root不变；跨目录评分须由独立adapter验证131格引用集合、两campaign原始seal和身份，再复用原算式。恢复adapter已通过原/恢复真实终态只读检查和初步4项合成测试；独立审查确认评分算式等价、strict支持绝对路径。冻结模型、entry/exit时间窗、argv与唯一复核绑定已通过真实metadata及4项测试。子代理提前生成v1控制后继续修改导致其失效，旧控制/集合保留不用于评分；root已建立score_adapter_v2.control.json及完整prediction_bundle_v2.json，131条原始绝对引用验证通过，未读取native实际。配对报告已测试并冻结，恢复score已生成；strict.0001结束身份检查再次拒收27B：三路全文摘要同为aebe0ac3…，均不同于冻结157479…，四项stat检查仍通过，未输出strict结论。该失败链唯一复核已耗尽，恢复链封存不再重试；评分仅作未验收诊断，不提交、不用于R41有效父基线。既有14177条分块日志对比定位唯一差异index3454、offset3621781504、长度1MiB；旧R37的index9960本次一致，不能归为固定坏块。只读系统记录显示该时段无System警告/错误、磁盘报告Healthy；四条内存报告两种型号/Speed4800与5600但ConfiguredClockSpeed均5600，仅作环境排查线索，不证明根因。合成缓冲自检已完成：固定seed每轮64MiB流式1MiB块、3轮OpenSSL/CNG摘要一致，耗时0.547秒；不是64MiB驻留复读或完整RAM测试，不能排除内存问题。现有guard仅在算法分歧时保存原始字节，三算法一致但相对参考块异常时缺少原字节。参考块差异捕获工具已在独立diagnostics目录实现，root修正硬性4MiB保存上限、1MiB单读上限、异常留证和尾部数据拒收，9项合成测试通过，要求流从offset0开始，并显式标记diagnostic_only及identity未评估；未接入冻结guard，未读模型，不能用该工具通过证明环境可靠。R40恢复链仍封存。已生成diagnostic_error_signature.0001.json，仅作已揭盲开发诊断：27B GPU TPOT/E2E中位APE由37.068%/33.965%降至11.186%/12.531%，四个小GPU模型则系统性更低估；不能以局部收益或达标格6→9抵消78格全项退化和身份失败。源码审计确认CPU build、driver submit、CP服务仍保留；新增确定性缺口为串行GPU阶段的全部controller流量被集中到首root，以及L2 ownership按GPU全局判断导致其他stage补充服务归零。root已在R40冻结源码运行root-stage-repro.0001：串行64B与1MiB两阶段交换数据量，1048640B controller服务始终全落首阶段，后阶段无对应服务，首阶段900ns/后阶段100ns不随归属变化。该结构复现不使用native。下一机制先在R42隔离候选修正逐stage资源归属及依赖，新增串行链/mixed-provenance合成回归；R42已修fallback混入CPU/H2D问题，按GPU-owned demand计数并区分缺失provenance与明确零字节，新增6项局部结构测试及既有35项集成回归均通过。root复测确认仅serving.py变化、实际导入隔离候选；首次误导入主区的5失败记录保留且不作为候选结果。structural_validation.0001.json保存41项通过证据，尚未冻结、未证明误差改善；真实lowerer双独立进程对照已触发4行prefill、ubatch2的两GPU串行stage：原HBM32036/47440B，R40全79476B归首stage，R42归回32036/47440B，各MMU继承原stage依赖，原lowerer字节/依赖相同；不改服务参数、不读目标时延拟合，不把R40提升为有效基线。真实analytical qwen2_decoder小图仅1个GPU memory candidate、88任务/5compact阶段，HBM51902B/L252254B归属匹配，未触发串行候选缺口；不能把合成缺陷直接归因为当前误差。root初筛完整131预测的21871个保存batch：18115单GPU访存stage、809多stage、397缺stage、2550无GPU访存stage；Qwen2.5/SmolLM2/TinyLlama全部保存batch为单stage，多stage集中Qwen3.5与27B GPU。独立existing_stage_coverage.json已核对：131格retained/returned均21871无批次遗漏；21474有stage（98.184%），397缺失标证据不足，所有可见batch任务细节均depth截断。809个multi候选全部为单root直接串行链，可见边数均candidate_count−1；按resource_ids保守分类，不推断隐藏bytes/work_units。R42不可作为前三组低估的普遍解释，后续并行审查host事件次数与R41几何可达性，优先级按完整覆盖证据决定。 host审查已定位HostOrchestrationProfile的batch_fixed与command_build_fixed/per_invocation聚合费用，LlamaGraphRuntime.prepare/commit只记录未计价生命周期且不改变DAG；不能仅凭unpriced标记追加整段host时间，须先界定check/reuse/reset/build/alloc/set_inputs现有费用归属与缺失条件。 已有源码审计仅支持两项证据不足：lifecycle事件到聚合host参数的ownership映射缺失，以及physical invocation group与真实backend graph split/compute调用计数未绑定。二者都不能直接推出应添加每节点dispatch或固定延时；下一步沿锁定源码建立符号调用计数。身份环境问题与模型机制修复分开处理。独立256MiB固定seed合成文件一次写入、同进程三次读取及三个独立进程各读取一次均未见差异，跨进程OpenSSL/CNG逐块和全文一致，且匹配重新生成的预期数据；只覆盖缓存读取，未覆盖全内存/实际模型大文件长期负载，根因仍未定位。继续性能验收前需要能解释/消除间歇数据异常的独立证据；后续诊断不得当作该链第二次复核，也不得自动采纳诊断所得预测。不得复制伪造worker/seal或调用只支持单目录的旧score入口；同链再失败不追加复核，原失败不追认。
| Engine指标 | APE中位数 | 场景误差P90 | 最坏APE | 绝对ms中位数 | 最坏ms |
|---|---:|---:|---:|---:|---:|
| TTFT | 40.010% | 62.067% | 67.904% | 68.702 | 2891.568 |
| TPOT | 30.599% | 49.319% | 56.471% | 2.308 | 125.894 |
| E2E | 29.851% | 48.537% | 60.568% | 411.220 | 25470.539 |

R34索引消费者修复没有改变共同111格的精度。各组误差方向不同：qwen25、qwen35与tinyllama的三项中位有符号误差均为负，qwen38 GPU三项均为正；smollm2的TPOT存在正负混合。不能用统一倍率或统一增加host开销解释全部部署。既有894 native请求及292条R22预测请求的逐请求E2E闭合通过，最大残差2.91e−11ms；独立取中位数不保证加法恒等式。三次batch中位数CV最大TTFT2.5313%、TPOT1.9039%、E2E1.7986%，只为现有样本诊断，不能抵扣模型误差或充当正式稳定性保证。

新静态覆盖审计检查R27/on的131条已存预测，共157类MMVQ签名：现有Q5_0/M1/K4096/N3072微基准在已表示签名中命中0；Q4_K/M1/K2048/N2048出现在SmolLM2/TinyLlama共45格、171200个任务。账本未表示部分仍为未知，不能把缺记录当成不存在；所有签名cache/native dispatch资格仍未知，不能因shape匹配直接准入成本。下一轮优先覆盖实际结构，零命中shape仅作工具链资格验证。

final norm设备/算子粒度修复与旧输出索引放置逻辑存在可复现的结构冲突：两路未改动的R33冻结源码在两层qwen3_5架构小图中，M64 prefill的索引与GET_ROWS消费者均在GPU；M1 decode的消费者转到CPU，但索引仍预先按旧final_norm.apply规则发往GPU，绑定依赖时抛出StopIteration。该复现不读取GGUF或目标时延。R33两路各20个27B CPU格同样记录StopIteration。已对on中qwen38_p128_o32_c1的完全相同冻结输入执行一次诊断，完整栈确认在_bind_output_index_upload的消费者查找失败；原20格未保存栈，仍不能将代表格诊断冒充20份原始栈。先按实际消费者修正索引放置和依赖，不能吞异常或按模型名称禁用GPU。非MTP在线target的GPU消费者与提交/传输归属已完成148项结构回归，当前准确性以本节R34完整结果为有效基线；逐ubatch的CPU生产—GPU消费关系及CPU图输入兼容性、重分配条件仍部分未知。这些是已有源码和任务图指向的候选缺口，不把系统性偏低直接归因于其中任何一项。MMVQ访存采用MMA输出tile效率的族混用保留为独立成本假设。已有nonflash、logits、sampling、launch不能整段再加；kernel_launch已归GPU frontend。P128/512/1536均整除ubatch64，不能默认有尾块；c不等于kernel M。

## 13. 当前执行及下一轮优化顺序（动态，原位更新）

1. **当前R37验证已证实的普通attention score头维度修复。** R34中普通Qwen2/Llama未提供专用attention descriptor时，score_heads错误回退到1。已有query_head_shard给出了实际物理头数，应据此计算QK输出、softmax行数/元素数、PV概率输入和工作集；QK/PV折叠FLOPs、GQA唯一KV字节及原生启动次数不按头数重复增加。Qwen2.5示例实际14个query heads，M64、KV256时score应为229376元素而非16384。此为源码与张量语义支持的修正，不依赖CUPTI时延或native答案。当前最小源码修正、TP层级fixture和真实stateful模板回归已完成，141项结构检查及8项冻结/失败处理检查通过；旧测试漏头维度的期望已按张量语义修正，原失败JUnit保留。
2. **先完成R39唯一新冻结的预测、评分与严格证据审查。** 有效准确性基线仍为R34/on；R35预测结束身份失败永久拒收。R37的唯一完整预测及一次恢复评分证据保留，但严格验收因cached GGUF header不一致返回证据不足，恢复链已封存；其改善数值仅作历史诊断，不再重试、不作为有效父版本。五模型完整复核、首次失败与恢复记录见本轮目录及第15节索引。
   R39从R37源码隔离修正retained头解析与摘要的同次字节绑定，不混入其他成本修改。120文件新冻结、完整身份预检通过；15项读取器回归及8项稳定字节等价检查通过，独立审查完成。131格唯一新预测已于2026-09-17 17:26:50 UTC自然退出0，结束身份检查通过；复用固定native，不运行原生推理。score.0001自然退出0；strict.0001自然退出4，仅为accuracy_failed，零证据不足、131格完整覆盖、70/393指标及6/131格达标。配对报告完整通过：54格改善、69格严格不变、8格混合。三项APE中位数28.434%/25.950%/24.468%；有条件保留作后续开发父版本，集中退化尚待机制解释，暂不提交，不追认旧预测。
   评分前的报告器v2已审查、27项合成检查通过并冻结（report_repair_v2.control.json，SHA e94b4937f8fa3d1ff07c86059ce69b6fddff6adcbe05911c278e8d893d6d5bd0）。它绑定预声明62格受影响/69格不变、R34基线、身份检查实际阶段，以及严格验收的精确131格、freeze/score来源、393指标和零不足证据。原控制与失败测试凭据保留。预测自然成功后依次执行一次score、一次strict及配对报告；任一失败按第8节定位处理，不自动重复运行。读取器缺口不等于已证明历史硬件读取异常根因。
   评分后按综合退化规则审查三项误差及六个模型/部署分组，区分误差超标与证据不足。只有证据闭合才决定接受、条件保留或回滚；只有确定性修复及误差改善验收被接受才提交。R36继续检查控制依赖，R38继续检查每head批量几何，两者不能混入R39。
3. **控制器费用依赖修复已以R40新冻结启动，R39为完整开发父版本。** 确定性缺口为CPU命令和H2D载荷仅因stage标为GPU，就被选为内存controller root，使GPU访问波次成为实际算子前的多余串行依赖。隔离候选按同一正工作量demand的实际GPU计算、缓存和显存归属选择root；保留服务量、参数、原前置依赖和controller/root并行关系，不依赖compact交接可能丢失的metadata。
   R39源码父本上的35项结构检查通过，独立只读审查确认当前frontend只含service而无bytes/work的路径被排除，真实command_processor载荷亦被明确排除；尚未证明误差收益。父链driver正在从R37适配为R39：真实源码目录为round_039/candidate_source，必须检查导入来源、完整manifest和runtime身份、自然执行终态、精确131格/393项与strict证据。首版合成检查通过后root发现报告字段、引用bytes/size_bytes规范化及下游旧字段兼容缺口；现已修正并按真实报告结构补测，74项相关检查通过，新增read_control和递归证据引用校验；不得因合成测试通过提前prepare/full。完整证据但误差未达最终目标的父版本可用于条件保留的后续开发，不等于单轮提交接受。R36冻结后的启动前guard发现bytes/size_bytes引用格式误报及source_kind旧标签残留，未启动子进程；prelaunch_failure.0001.json证明SHA和长度均一致。旧冻结保留，R40仅修复入口比较和标签，继承已审查R36隔离候选及R39完整源码；49项驱动检查、真实prepare及verify均通过，full.0001已启动131格预测，不运行native。父工具来自冻结R39与候选未变helper同源，非主区回退；不混入R38几何修复。 R40评分前工具由两个代理分工：报告器复用R39已验证统计并按六部署分组，启动器区分准确性失败与证据不足。报告器与启动器已统一接口并通过23项联合检查；root补齐strict内嵌状态快照的内容摘要校验与后续live state免重复读取，独立复核已通过，report_controller.control.json已在评分前冻结（SHA 998641baa6d004a305d6c87329fa6b2f9bcfd81e1dbae2e4a707f83af0689723），未读取候选评分；真实source_origins为120文件清单，只有serving.py变化，不能误当成仅1条记录。
4. **后续nonflash候选先修每head批量几何，精度/转换另列。** R40运行期间只读兼容性审计确认，后续移植限cost_models.py、planner.py及预测入口三文件；预测入口须逐段合并，保留R39同次读取绑定，serving.py与retained_header_stream.py不得由旧R38覆盖。源码/运行时资格按实际绑定重新核对，不为通过检查替换预期摘要；R41已从R40的120文件manifest建立未冻结候选，仅合并三文件，serving.py及retained_header_stream.py字节保持；三处后端源码资格摘要与R40实际绑定一致。正确导入R41三模块后190项结构检查通过（geometry-port-validation.0003.xml）；第一次虽190通过但误导入主区，第二次缺tests辅助路径，均保留且不作移植通过证据。结构通过不代表误差改善，R41未冻结、未预测，仍等待R40验收。 锁定源码证明普通统一KV、F16缓存、每stream实际M≥64、计算类型环境覆盖未设置/auto时走cuBLAS：QK每head形状(M,d,L)，PV为(M,L,d)，batch=Hq；GQA按query-head组映射KV head并生成指针，PV的原生lda取缓存容量C而非当前view长度L。当前二维折叠保持了总FLOPs，却未表达batch与独立输出tile数量。最小候选只让现有未验证tile/wave代理使用每矩阵几何，保持useful FLOPs、score、Hkv唯一KV字节和事件/launch数不变，并同步更新fresh compile与动态replay；不能把代理值称为真实cuBLAS CTA。大M QK有K→F32，PV有P→F16和结果→F32的转换路径，F32 compute也允许TF32数学模式；这些转换、指针与实际精度成本留作独立候选，不先塞进几何修复。小M的MMVF/MMF、量化KV、flash、非统一KV、未证明的TP/padding和环境覆盖不套用该规则。R38已从R37源码快照建立隔离开发副本，正在分别实现成本几何与fresh/replay接线；这仅是源码父本，尚非有效验收基线。第一版进一步限定单卡TP1且折叠宽度严格等于Hq×d，避免把已知TP padding不一致带入。配置、成本和planner已在隔离副本集成，190项联合结构回归通过，独立复核提出的资格与验证问题已闭合。适配40项检查绑定历史采集compute type、CUDA/llama模块及源码，不把缺失记录当作未设置；llama模块遗漏的7项失败复现已保留。新增GQA整除及历史fast-FP16/转置F16V布局资格，PV的native lda=C不外推到未证明硬件。成本94项检查保持useful FLOPs、唯一字节和launch数；planner56项已核对资源需求、完整cost/phase元数据与fresh几何，真实TopologyAwareBatchCostProvider入口验证第二次exact-cohort缓存命中、lowerer不再执行且结果与makespan等于fresh。独立复核指出的旧断言缺口已补齐，旧凭据保留；现有M1动态serving准入未扩展，跨L只证明手动成本重放和fresh一致，不宣称实际动态serving命中。131格静态输入均具备该来源和采集证据，实际invocation是否满足M、设备和布局仍由planner逐次核验。该副本尚未冻结或预测，未证明误差收益，仍按R37有效比较→R36依赖修复→批量几何的顺序验收。
5. **R31微基准已封存，采集修复不是本轮语义修复的前置门。** 新Q4 wrapper实际路径与独立数值资格通过；100进程/1200秒计时因预算耗尽自然结束，6有效、7拒收、87未执行全部保留。B/AB有完整kernel但缺Runtime API与external关联，另1样本warmup记录端点比检查端点早而不足0.5秒；无成本参数准入。未来只做独立的不计时采集诊断，再修复观测兼容性与首次拒收后停止派发、重复历史校验的流程缺口；不覆盖或继续重试旧冻结。
6. **保持顶层关系优先和逐格目标。** 全131格已存stage均未命中怀疑的mixed-backend serial_fallback；66→65的27B GPU配置是65 GGUF块含1 MTP辅助块到64主干层加lm-head的已解释映射，不作为错配处理。CPU图已有聚合费用，不能再重复加整段host wall。固定131格三项全部严格<10%前A未通过；A后再做独立B。正确语义但尚无精度收益的修改保留工作区，未达到目标且仍有可执行方向时自动续轮。

## 14. 冻结、复用与循环预算（动态，原位更新）

- 固定native、历史冻结/预测/评分不可变；复用actual，只重跑simulator。完整文件SHA不能由payload摘要、header或stat代替。失败格立即诊断并补测，保留原attempt和根因/修复关联；源码或配置改变后按新冻结重跑，未变格复用需证明等价，不能拼接不同版本或悄悄重跑至成功。
- R24来自R23冻结113个源码字节继承及1个已提交adapter替换，完整预测/评分已结束，139项详细证据逐字节归档并分3卷上传。
- R25两路相同源码、相同成本开关和配置，仅末层选行处理不同；来源为R24冻结加已提交候选文件，不混主区历史WIP。protocol、freeze receipt、controls已保存；评分由262终态屏障约束。R25现已因on启动校验失败封存，133项冻结/预测明细已逐字节归档并分3卷，driver/helper/冻结源码继续不可变。采集合成证据必须核对关闭凭据及实际进程退出；存在任何新轮仿真或native/probe进程时仍禁止并行执行。
- 阶段复盘：最新有效精度仍为R34；R35预测末尾身份失败维持拒收，R37进入有完整复核证据的评分恢复，R36等待有效父版本，R38隔离实现190项联合结构回归通过。原有分块诊断已结束，不将“必须先证明整机无故障”作为单次哈希复核成功后的统一前置条件；恢复范围严格按第8节区分。GGUF同次读取绑定修复仍独立保留，尚未进入R37，不把它宣称为旧异常根因。阶段轮数/时间窗为复盘点；结构工作可并行，真实采集与重仿真串行，子进程自然退出。
- 仅在确定性缺口优化与基线/候选误差改善验收完成后，一次提交相关源码、测试、任务书及小证据；中间更新留在工作区。大JSON/trace逐字节分卷。不上传GGUF/DLL/EXE/OBJ/cubin或整棵复制源码，构建脚本和身份凭据可复现。

## 15. 最近结果与交付位置（动态，原位更新）

以下路径根为artifacts/development/native_long_grid_135_20260915/。

- stable_native_dataset.json、optimization_loop/state.json：固定native与循环状态。
- optimization_loop/round_027/report.json、grouped_paired_report.md/json、review_outcome.json：历史262终态后的配对评分、共同129格与覆盖变化。
- optimization_loop/round_024/REPORT.md、identity_repair_report.json、repaired/errors.0001.json：历史完整评分、104格精确复现及失败。
- optimization_loop/round_027/heatmaps.0001/：off、on与APE差值的PNG/SVG热图；X为失败，短横线为固定集之外。
- optimization_loop/round_028/static_kernel_coverage.0001.json、verify_static_kernel_coverage.py：131预测静态签名、微基准覆盖及独立重算入口。
- optimization_loop/round_030/protocol.json、freeze_receipt.json、controls.json、preflight/：真实冻结与lock凭据；postprocess/：262终态后的热图及干净归档入口，尚未处理真实R30结果。
- optimization_loop/round_035/、round_036/：R35保存141项结构回归、8项原驱动检查、117文件冻结、131条预测与结束身份失败；identity_diagnostic保存11项诊断回归、一次4MiB/1MiB读取对照和逐块结果，原故障未复现亦未解释，尚无有效评分。R36保存控制器root错位复现、35项结构回归及8项新版驱动检查（含原始异常保存），等待有效父轮。两轮均未通过误差改善验收，不提交。
- optimization_loop/round_034/：实际索引消费者修复的未提交内容冻结，270项结构回归及原20失败格定向补测；processes/full.0001/finish.json记录完整131格自然结束。on/errors.0001.json、report_repair.json/md与strict_goal.0001.json保存冻结评分、共同111格/333项unchanged、20格覆盖恢复及A门5/131结果；heatmaps.0001/提供三项SVG热图和HTML/Markdown报告。报告控制在评分读取前冻结，10项合成测试通过；本轮未通过精度改善验收，不提交。
- optimization_loop/round_033/：前端结构对照的新驱动，两路共用审核过的身份诊断补丁，逐文件锁定Git来源；73项合成回归与独立复核、四份131格真实freeze/lock预检通过；startup_identity_observation.0001.json保留启动guard的小PDF身份失败；startup_resume_decision.0001.json记录一次有界恢复；现两路均已131终态、自然退出，262屏障通过。diagnostic/output_index_consumer_repro.py、boundary_scan及exact_failed_cell.0001分别保留小图、56例边界和一个完全相同冻结输入的原错复现；不等同于历史worker逐格栈。
- optimization_loop/round_032/：已拒绝的真实冻结，preparation_closed.0001.json及preparation_failure_evidence.0001.zip/json保存144项逐字节核验的失败证据；diagnostic/仅为一次后续独立读取，不能追认原失败。
- optimization_loop/round_031/q4k_timing/：preparation_validation.0002.json保留编译、身份与69项主机回归；qualification.0001/保留已通过的新wrapper实际路径、原始数值和独立校验；run.0001为既定预算计时实验，服务成本参数仍未准入。
- optimization_loop/round_024/full_predictions.json、full_scores.json、detailed_evidence.parts.json：先预测后评分与3卷恢复入口。
- optimization_loop/round_025/protocol.json、freeze_receipt.json、controls.json、execution_closed.json：已拒绝批次的冻结、131配置错位重导和退出记录，未评分。
- optimization_loop/round_024/mmvq_device_probe/r5_device_code_compare/、r6_shared_abi/：设备代码及共享ABI证据，均无性能系数。
- optimization_loop/round_026/target_capture_run.0001/、target_capture_ex_run.0001/：旧API拒收及新ExC实际路径通过的完整原始记录；wrapper_correctness_v2_run.0001/为已通过的数值记录；mmvq_wrapper_launch_match/为已通过的连续布局动态对照与QI勘误。
- optimization_loop/round_023/optimization_direction_metric_audit.md、optimization_direction_source_audit.md、host_cost_ownership_audit.md、mmvq_memory_geometry_audit.md：已纳入任务书的参考方向审计。
- optimization_loop/round_022/REPORT.md、round_023/REPORT.md及各轮归档索引：历史误差、退化和失败保留。
