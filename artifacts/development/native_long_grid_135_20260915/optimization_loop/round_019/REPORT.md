# R19：计时证据仍未准入，自动进入 R20

本轮没有修改 simulator 成本模型，没有重测目标 LLM，也没有生成新误差预测。最新准确性结论仍是 R18 同冻结 20 格：0 格三项同时小于 10%；A 失败，B 未验证。

## 已完成与限制

- 独立 CPU 微基准修复了间接 DLL 加载、CPU observation 类型以及编译后 schema 检查的时序；测量过程等待自然退出。原版、r4、r5、r6 的失败与原始记录均保留。
- r5、r6 各完成 3 进程、18 case-process、36 stage、2304 个 steady 样本。数值、身份、36 个频率观察、36 个 observer 门及 12 个跨进程中位数组均通过；stage P90/P10 分别只有 35/36、34/36 通过。按原定整组门限均拒收，输出成本系数为 0。
- r5 的编译 fixture 实际在 series 后验证，不能写成预先通过；r6 已将这个检查落实为创建 series 前的必需步骤。每个 revision 只允许预定的一组测量，不通过即保存失败。
- 词表中间尺寸已被用于质量诊断，不能再称为最终独立盲测。源码等价的外编译 candidate loop 不能称为原 common.dll 二进制计时。
- GPU 源码诊断识别出 MMVQ 使用 DP4A/解包/归约，而成本模型仍用 MMA tile-wave 去限制带宽的问题；MMQ 的 int8 MMA 类别本身正确，实际共享内存、驻留和 stream-K 仍缺成本证据。没有把源码几何直接变成经验倍率。
- 图间隙 A/B 的源码、入口、冻结/加载器/数学检查已准备；主机数学 8160 项通过，尚未编译或执行。下一轮复制到新目录冻结，旧文件不覆盖。

## 验证与续轮

根代理回归：CPU identity/quality/loader 27 项通过。固定 native 的 131 文件、131 格、894 个正式请求 SHA 核验一致。拒收包络没有进入 simulator 参数。

R20 限定测试两个可证伪假设：采样前的 reset 写入路径是否改变波动；图调用后的 D2H/数值检查/JSON 是否改变下一调用的状态。同时审核可独立完成的 MMVQ 源机制修正。两路实际计时串行，完整分母与失败保留；不修改目标与质量阈值。
