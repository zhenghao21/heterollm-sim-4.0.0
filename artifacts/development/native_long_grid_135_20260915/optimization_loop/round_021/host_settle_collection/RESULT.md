# R21 host-settle 首对结果审计

本轮 **未完成，short 与 settled 均被质量规则拒绝；不能宣称 1000ms 预热有效。新增或拟合系数为 0**。

控制器于 2026-09-16 16:43:52（北京时间）启动，16:49:34 以 `failed_or_incomplete` 结束；原因是首对 root 审查窗口到期。已完成 **6/18 阶段（33.33%）**，包括 4/12 个 native 进程和 2/6 个导出；两条件各完成 1/3 对。剩余 12 阶段均没有完成回执，分母完整保留。6 个已执行阶段均返回 0、自然退出并通过结构验证；这些状态不代表通过计时质量要求。最终时钟重置返回 0。

`continue.json` 的文件时间为 16:51:22，比控制器结束晚约 107.27 秒。控制器的超时退出记录与后续阶段缺席一致，**迟到的继续文件未被消费，不构成已经执行的续跑许可**。本审计未恢复或重启控制器。

| 首对指标 | short（0ms） | settled（1000ms） | 固定要求 |
|---|---:|---:|---|
| direct 主机中位数 | 64.950 µs | 64.000 µs | 仅描述本次观察 |
| direct P90/P10 | **1.697619，失败** | 1.045354 | ≤1.50 |
| profile 主机中位数 | 71.700 µs | 76.900 µs | 用于扰动比较 |
| profile 主机 P90/P10 | 1.155304 | 1.095688 | 描述指标 |
| profile GPU kernel-union 中位数 | 15.6325 µs | 15.5520 µs | 仅 Nsight 时钟统计 |
| profile GPU P90/P10 | 1.010502 | 1.010336 | ≤1.50 |
| profile/direct 主机中位数扰动 | 10.39261%，通过 | **20.15625%，失败** | ≤20% |
| 正式样本所用时钟读数：direct / profile | 2392 / 2392 MHz，通过 | **2340 / 2355 MHz，均失败** | 2400±30 MHz |
| 独立进程对完成数 | 1/3 | 1/3 | 3/3；跨进程中位数偏差≤5% |

扰动按 `abs(profile中位数-direct中位数)/direct中位数` 计算。4 个 native 进程各有 30 个正式窗口且均有时钟前后读数，最大包围间隔不超过 5.0909ms，满足 25ms 的覆盖要求；settled 失败来自实际频率越界，不是采样覆盖缺失。时钟证据是采样窗口，不是逐内核连续波形。

4 个 native 进程各保留 36 次记录调用、30 个正式样本、3 个全阶段数值检查块；每进程检查 6,291,456 个值，mismatch/nonfinite 均为 0，模块稳定。两个 profile 的已存严格 trace 均给出 36 次调用链完整、288 个实际映射内核、正式分母 30、源路径族匹配且无 warning。数值检查只覆盖规定的 first/post-warmup/post-formal 检查点，不能描述为每次调用均检查输出。

settled direct/profile 分别实际执行 14,492 / 11,967 次 settle 循环，QPC 时长为 1000.2171 / 1000.0429ms；short 均为 0 次。独立核对确认 settle 位于 first 之后、warmup0 之前。profile settle 内核单独保留：short 为 0，settled 为 95,736；均未计入 36 次调用或正式估计。QPC 主机时间与 Nsight GPU 时间没有跨时钟相减。

settled 的 direct P90/P10 在本次观察中确实更低，但两条件的有效时钟域不一致，settled 的 profiling 扰动越过预设门槛，且只有一对进程，无法评估三对之间的重复性；因此不能据此接受稳态改善、CPU 专属原因、性能收益或任何校准系数。两组仍为 `diagnostic_quality_rejected`。

审计只读取现有证据：核对冻结文件及 11 个 collector 输入哈希、6 阶段回执和计划身份、4 份原始记录/审计/sidecar 哈希、36/30 分母、settle QPC 边界、主机中位数及正式时钟采样。大型 trace 采用已存汇总与有界首尾字段核对，未重载 306MB JSON、重新归属 SQLite 或重算其全文件哈希；其引用保留原回执值。未运行 native、profiling、时钟写入或重 CPU 分析。

后续仅建议：先归档这次失败和未执行分母，低成本检查已有时钟/功耗证据，再决定是否另行预登记后续实验及独立重复；不要重启已结束控制器或放宽本次质量门槛。建议未执行。

冻结 SHA：`4c4946db15968602476fb967b772e8f748eb9c64f27253a9afcf2c29aff6c2fc`。详细字段与证据引用见同目录 `result_summary.json`。
